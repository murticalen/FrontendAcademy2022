export default function About() {

    const v = Math.random()
    console.log(v)

    return (
        <main>
            This in an about page for SofaScore academy. It is static without data.
        </main>
    )
}